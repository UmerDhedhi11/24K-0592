Q1: 
Because the given input is out of range of storage for that specific datatype this is why the computer outputs a random value.

Q2:
#include <stdio.h>

int main()
{int num1, num2, temp;
 printf("Enter 1st number: ");
 scanf("%d", &num1);
 printf("Enter 2nd number: ");
 scanf("%d", &num2);
 printf("Previous order of inputs: %d,%d \n",num1,num2);
 temp = num1;
 num1 = num2;
 num2 = temp;
 printf("After swapping: %d,%d",num1,num2);
}

Q3:
#include <stdio.h>

int main()
{
float sal, tr, tx, r_sal;
printf("Enter your salary: ");
scanf("%f", &sal);
printf("Enter your tax rate: ");
scanf("%f", &tr);
tx = sal*(tr/100);
r_sal = sal - tx;
printf("$ %f will be cut due to tax \n", tx);
printf("You will receive $ %f \n", r_sal);
}

Q4:
#include <stdio.h>

int main()
{
float avg, fuel, p1, p2, totalp;
float total = 2414.0;
float forwardp = 118.0;
float returnp = 123.0;
printf("Enter car's fuel average: ");
scanf("%f", &avg);
fuel = total/avg;
p1 = forwardp*(fuel/2);
p2 = returnp*(fuel/2);
totalp = p1 + p2;
printf("Fuel consumed: %.1f litres \n", fuel);
printf("Total fuel cost: $%.1f", totalp);}

Q5:
#include <stdio.h> 

int main()
{int prcpl, tp;
float ir, si;
printf("Enter your Principal that should be between Rs.100-1,000,000: ");
scanf("%d", &prcpl);
printf("Enter your rate of interest that should be between 5-10 percent: ");
scanf("%f", &ir);
printf("Enter the time period between 1-10 years: ");
scanf("%d", &tp);
si = (prcpl*ir*tp)/100;
printf("Your simple interest for the provided time period is: Rs.%.2f", si);
}

Q6:
#include <stdio.h>

int main()
{int x1, x2, y1, y2;
float grad;
x1 = 5;
x2 = 3;
y1 = 4;
y2 = 2;
grad = (y2 - y1)/(x2 - x1);
printf("The gradient is %.3f", grad);
}
